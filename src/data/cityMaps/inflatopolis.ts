import { buildCityMapFromLayout } from "./layoutTools";
import { INFLATOPOLIS_LAYOUT } from "./inflatopolisLayout";

export const buildInflatopolisMap = () => buildCityMapFromLayout(INFLATOPOLIS_LAYOUT);

